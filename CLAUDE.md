- Communication style: No emotional tags or exclamation points. Just code-related information. Don't be "agreeable", test claims, be rigorous, evidence based, and rational.
- Commit & PR hygiene: No claude co-authored messages.
- run all tests and commands that would need dependencies with `nix develop -c`

## Which branch is real

`main-0.6` is the line that ships. v0.14.8 was built from it. **`main` is a dead end**, frozen since the split at `c589a6b` — two commits after it, both substantively present on `main-0.6`, and none of the later work (no `packages/` directory, no `test` script, no FSM). The `v0.14.8` tag was repointed 2026-07-27 at the verified build source (`7b86951`, on the `fix/flash` lineage) — the tag itself is now correct, but it is not an ancestor of `main-0.6`, so `git describe` on the trunk still answers nothing. The remote default branch is `main-0.6` (verify with `git ls-remote --symref origin HEAD`, not the local cache — a clone from before the switch needs `git remote set-head origin -a`); the dead `main` branch still exists locally and on the remote.

Check your branch before reading source. Reading `ui/src/` from a `main` checkout gives answers that do not describe the shipped app.

## Read before changing connection, presence, or carrier code

`MAINTAINABILITY_ASSESSMENT.md` (repo root) is `Status: HISTORICAL` since 2026-08-06 (the §10 meta-review ratification — see the meta-review fact bullet below): a dated record of the 2026-07→08 maintainability effort, no longer corrected in place. Its §3 defect inventory described the v0.14.8 build, and its items were worked through the nine rounds recorded in the fact bullets below — read it for why the current seams exist, not as a live defect list. The live authorities are the tests and the bullets below.

Never trust prose over code in this repo — two audits (2026-07 assessment §3.10, 2026-08 retro §7.3) both found false assertions presented as current fact, and the second found them in documents the first had corrected. The status rule (decided 2026-08-03, resolving the contradiction between working agreement 3 and Phase 0 item 6 that retro §7.3 exposed): **active documents carry no status ritual — they are corrected in place or deleted; a superseded or historical document MUST carry a `Status: SUPERSEDED (see X)` / `Status: HISTORICAL` header**, following `docs/WEBRTC_PEER_CLAIM2_DATACHANNEL_GATING.md`. `docs/WEBRTC_CONNECTION_PLAN.md`, `docs/CONNECTION_LIFECYCLE_PLAN.md`, and `WEBRTC_CARRIER_ANALYSIS.md` are HISTORICAL — their headers name what supersedes them. Verified sound at the retro: `packages/webrtc-peer/README.md` (6/6 spot-checks). When code and prose disagree, the code is what ships.

## True today (facts, not goals)

What may live in this section, and how it stays true (decided 2026-08-03, after the same three classes of claim went stale here repeatedly — retro §7.3): entries are **records of landed changes**, anchored to a date/merge (past tense is always safe), and **present-tense claims only when they name the file or test that enforces them**. Three classes are banned outright: (1) derivable counters — test counts, line counts, coverage numbers; the authority is the run's own output, and `ui/src/__tests__/claude-md-drift.test.ts` rejects the greppable forms; (2) repo/world-state snapshots — tag positions, remote heads, "no file exists"; write the check procedure instead of the answer; (3) unanchored negations — "X still runs…", "nothing writes Y" — which the very fix silently flips; a negation must name what retires it or be dated as an observation. When a chain of work changes a fact stated here, its closing doc-sync commit updates this file — the reviewer checks that the sync commit exists.

- **Post-Phase 3 carrier facts** (landed 2026-07-29, merge `492197b`; adversarially reviewed; field validation landed same day as Phase 3.5 — the release gate is satisfied). The FSM is the **only** WebRTC carrier — SimplePeer is deleted, not just unreachable: no `simple-peer` dependency, no `SimplePeerTransport`, no impl preference (`resolveWebrtcImpl`/`webrtcImplFor`/`peerImpl` decisions are gone; `webrtcAvailableFor(peer)` — does the peer hold the `sdp-fsm` cap — became the one WebRTC gate; since Round 3 (2026-08-04) the one gate is `decideWebrtcEligibility` in `transport/carrier-coverage.ts`, with `webrtcAvailableFor` as its capability conjunct, applied at both video handshake ends). Screen share runs on two FSM transports (out=sharer, in=viewer) over the capability-gated `SdpFsmScreen` signal type; incoming screen signals are routed by the sender's declared role (`decideScreenSignalRoute` in `transport/screen-signal-policy.ts`), never by connectionId, because each side's FSM allocates its own. The screen InitRequest/InitAccept handshake and all pending-accept maps are deleted — the FSM acceptor creates state lazily from the first offer; only `_pendingInits` (video initiator pacing) survives. `SdpData` is retired in the wire contract (`emits:false/parses:false`, explicit drop arm). Declared interop change: screen share does not interoperate with v0.14.8 in either direction; video with v0.14.8 still works; no WebRTC at all with peers lacking the `sdp-fsm` cap. Auto-flip (`decideAutoFlip`, `decideCarrierSwitch`, `auto-flip-policy.ts`) is deleted per the unscheduled-table decision. `_screenShareStreams` is wired (set on remote-stream, cleared on close/error/leave/disconnect). The stale-connection supervisor is kept but always stands down in production — every live transport owns recovery.
- Signals is a **module-layer fallback**, not a `PeerTransport` — `ui/src/transport/types.ts` says so explicitly. Since Phase 4, `PeerTransport` **is** the type annotation for the store's three transport fields, so it is a real constraint, not a decoration.
- **Post-Phase 4 facts** (landed 2026-07-29; adversarially reviewed — F1/F2 resolved on the branch, mutation-verified — and merged `--no-ff` as `cd28f22`). The `getRTCPeerConnection` escape hatch is **deleted** — the pc never leaves the transport. ICE forensics are transport-emitted `ice-diagnostic` events (listeners attached at `onPeerCreated`, one set per peer session, aborted on close/replace); the store keeps `_iceTimings` and the `IceDisconnectedAt` bookkeeping from those events, and the poll-until-pc-appears attach machinery is gone. Per-peer control is typed: `prioritizeAudio(peer, {videoMaxBitrateBps})`, `refreshMediaForPeer(peer, stream)`, read-only `getIceConnectionState(peer)` (stale-net input only); `recreateDataChannel`/`restartIce` deleted (zero callers). `statsFor(peer)` (`transport/carrier-stats-policy.ts` over `carrierFor`) is the one carrier+stats answer — the stats panel no longer hand-branches, and its carrier keys on `connected`, not slot existence. `decideAudioLink`/`buildPeerLinkSnapshot` (`ui/src/peer-link-policy.ts`) own the audio-link roll-up: **observed media flow beats the signals-reachability veto** (a pong outage no longer marks a peer with live WebRTC audio 'absent'); UI copy reserves "connected" for ICE+DTLS (`observersHearing`/'heard by', 'not reachable by you'). The `'start-ice-monitor'` route handler is renamed `'signaling'`. No wire-surface change; `PeerLinkSnapshot` semantics pinned by tests.
- **Post-Phase 2 presence facts** (landed 2026-07-28, branch `feat/phase-2-presence-clock`). The **present** predicate has one authority: `computePresentPeers` (`ui/src/presence-policy.ts`), exposed as `StreamsStore._presentPeers` — ping-fresh OR media-flowing, re-evaluated on a presence tick every `PING_INTERVAL`. Chimes, tiles, all three grid counters, `globalPresenceSet`, and phantom exclusion key off it; join/leave sounds are store events (`peer-joined-presence`/`peer-left-presence`) decided by `decidePresenceSoundEvents` with a 2-tick leave dwell, so a presence flap shorter than the dwell makes no sound. `StreamsStore.clock` (`ui/src/clock.ts`) is the single time authority — every timing site in the store and the controllers' `peerLastRecvMs` stamps route through it; wall-clock remains only where cross-peer wire timestamps are compared (jitter/transit EWMAs) and in view-paint timers. The presence-family constants live in `presence-policy.ts` with one window per predicate (`PRESENT_STALENESS_MS`, `MEDIA_LIVE_WINDOW_MS`, `OBSERVER_FRESHNESS_MS`, `LAST_SEEN_FRESH/GONE_MS`, `PRESENCE_LEAVE_DWELL_MS`). **Not consolidated by Phase 2**: transport-layer graces (ICE-disconnected 15s, SDP timeout 15s, init retry 5s, ICE-monitor attach budget), the handshake TTL (`PENDING_HANDSHAKE_TTL_MS` 20s), voice/filmstrip internal stats windows and local send stamps, and the filmstrip display-hold TTL (documented as paint, not liveness). The lobby's separate presence model (and its undocumented twin in `presence-app.ts`) was consolidated 2026-08-03 onto `ui/src/passive-presence.ts` — see the post-retro facts below. Declared behavior changes: signals-audio liveness window 2000→3000ms in `audioLinkFor`; a signals-only peer with stale pongs now gets a tile; leave chimes are dwell-delayed ~4s.
- **Post-Phase 5 facts** (landed 2026-07-31; adversarially reviewed — item 1 interim F1–F3 applied, items 2–6 review's blocking F1 sort-before-truncate fixed and mutation-verified — and merged `--no-ff` as `e670051`). The diagnostic pipeline is a declared product surface: `SIMPLE_EVENT_TAXONOMY` (`ui/src/logging.ts`) declares every `SimpleEventType` as `emitted` or `historical`, `satisfies`-checked and enforced by `__tests__/event-taxonomy.test.ts` (emission-grep both ways + inline snapshot — keep emission sites as literals on `event:` lines, either quote style). `AddStream`/`SdpTimeout` are deleted (never emitted on any ref); `Pong` is historical. Outgoing `DiagnosticResponse` truncation is `buildDiagnosticSnapshot` (`ui/src/diagnostic-snapshot-policy.ts`, pure, table-tested); a cut snapshot declares `truncated: {events, customLogs}` drop counts (absent = complete; bound is by-count, not by-bytes; the policy sorts both collections by timestamp before the guard, so the drop is provably oldest-first even though the caller's flatten is agent-grouped — review F1). `DIAGNOSTIC_WINDOW_MS` (`logging.ts`) is the gather window — a reporting window, NOT a liveness predicate; don't borrow it. `DiagnosticSnapshot` timestamps are the reporting node's wall clock, `generatedAt` the cross-node anchor — never compare across snapshots raw. `logs-graph.ts` is demoted: new event types get no render arms (default pink arm is intended); deleting the file is sanctioned if upkeep grows. Deleted as double-dead: `ConnectionConfig.diagnostics`/`_logDiag`/the store's `DIAG:` filter, and `TransitionRecorder` (declared library-surface deletion). The flash investigation is closed as stale: `fix/flash` is HISTORICAL (`f8329b6` commits the missing module so the imports resolve; archive-only, never merge); if flashes recur, instrument via the PresenceLogger pipeline, not a parallel channel.
- **Post-Phase 6 facts** (landed 2026-07-31; adversarially reviewed — review F1's screen-share slot-apply coverage gap closed in the fixup and mutation-verified by the reviewer, F2 prose scoping applied — and merged `--no-ff` as `fb4c776`). The ambient world is one injected record: `StreamsStoreDeps = { clock, storage, bus, transportFactory, mediaDevices }` (`ui/src/store-deps.ts`); the constructor takes it and `static connect` builds the production record — the ONE binding site. The `roomClient`/`roomStore` fields are deleted: `bus` (`{ myPubKey, onSignal, sendMessage }`) is the only wire path, and the six `roomClient.client.myPubKey` re-encodings collapsed onto `myPubKeyB64`. Every ambient storage read AND write goes through `storage.{local,session}` with live-read semantics preserved (`dtlsStallTimeoutMs` stays a per-transport construction snapshot; `iceServers`/`trickleICE`/`iceTransportPolicy` stay live closures — the wiring tests pin the distinction). `transportFactory(purpose, options)` yields the three transports (`purpose` is a new declared seam element: `'media' | 'screen-share-out' | 'screen-share-in'`; production ignores it). Still ambient by declaration (browser-only, harness-covered): Mic/Camera/screen-capture `getUserMedia`, the canvas keepalive, static connect's page-lifecycle listeners. `start()` and `disconnect()` now execute under vitest in node; the wiring suite (`ui/src/__tests__/streams-store-wiring.test.ts`, fakes in `ui/src/store-deps.testing.ts`) drives the real glue — slot lifecycle for the media transport AND both screen-share directions (the screen half was review-F1: the first cut left it mutation-invisible), including the no-event FSM replacement, stale-event supersede guards, the trailing duplicate `closed`, the vanish-keeps-slot bounded exception, and the `_screenShareStreams` mirror; `_signalsTargets` from real slot writes; ping/pong and SdpFsm/SdpFsmScreen routing as actual `sendMessage` traffic; start/disconnect symmetry (destroy-exactly-once — a duplicated `mediaTransport.destroy()` was deleted as a declared cleanup). Scope: this validates the transport-event and signal wiring; media acquisition/encoder paths stay browser+harness territory. **Phase 6.5** (landed 2026-08-03; adversarially reviewed — no findings; the reviewer re-ran the full verify suite and harness and confirmed two formerly mirror-invisible store mutations now fail the specs — merged `--no-ff` as `13456a2`): both field harnesses construct the real `StreamsStore` over the `bus` seam (BroadcastChannel with remote-signal semantics) and the mirrored dispatch glue is deleted — the nightly executes the store's actual glue against real RTCPeerConnections, including the real ping/pong presence, caps propagation, the init handshake, and `_ensureOutgoingScreenShare`. Declared harness stand-ins (each harness header): static connect's roster/ping-loop arming, `connect()` = real `_syncConversationPayload` activation, screen `share()` = acquisition injection (Electron-only `getUserMedia`), and the `epoch0` supersede seam. Conductor-level (real-Holochain) validation remains manual.
- **Post-retro facts (2026-08-03, the §7.5 round; five stacked branches, adversarially reviewed).** One passive-presence authority: `ui/src/passive-presence.ts` (pure `applyPassivePresenceSignal`/`prunePassivePresence` + `PassivePresenceTracker` on the injectable clock) is the ONE implementation of passively-observed room presence — lobby room cards and the home screen both consume it; the window is the named `PASSIVE_PRESENT_STALENESS_MS` (`presence-policy.ts`, 5×`PING_INTERVAL`, doubles as sweep cadence). The no-ambient-clock pin covers the file list enumerated in `no-ambient-clock.test.ts` — the test is the authority; at this round that was streams-store, passive-presence, room-online-agents, peer-stats-panel, and presence-app (the last with a documented relaxation for argument-taking `new Date(ts)` formatting). Stray thresholds named: the stats panel's flow window reads `MEDIA_LIVE_WINDOW_MS` (declared change 2000→3000ms), voice's `PLAYOUT_ANCHOR_MAX_AGE_MS` is declared NOT-liveness, `peer-link-policy` shares `STALE_CYCLES_REFRESH_THRESHOLD` with track-health. `noUnusedLocals`/`noUnusedParameters` are on in BOTH workspace tsconfigs inside `verify` — **the eslint toolchain is broken outright** (the `lint` script's parser cannot load; repair is folded into the EOL-deps decision, assessment §7.4 item 4). Four view-teardown leaks fixed and pinned by `ui/src/__tests__/view-teardown-symmetry.test.ts` (per-file jsdom — jsdom is a ui devDependency, vitest inlines the `@holochain-open-dev`/`@shoelace-style`/`@theweave` ESM packages): keydown removal, asset-store unsub, `super.disconnectedCallback()` in room-view AND presence-app (controller-spy pins), and static connect's single released `allAgents` subscription. The status rule is decided (see the "Read before changing…" section above / working agreement 3): active docs corrected in place, superseded/historical docs MUST carry `Status:` headers. `tests/` holds the committed signal-latency tryorama rig — a measurement instrument (`npm run test:latency -w tests`), excluded from the workspace `test` script; its "not a gate" status was retired by the 0.7 step-1 chain below, which made `tests/src/room` gate content (`tests/README.md`).
- **Round 3 facts (§8; landed 2026-08-04, eight branches merged `--no-ff`, final merge `d161423`; full working-agreement-9 cycle — three review findings, item 1 amended in review, all mutation checks reproduced by the reviewer).** Connection-teardown cleanup has ONE home: `closeCleanupPlan` (`ui/src/transport/close-cleanup-policy.ts`), a pure (target × via × outcome) table executed solely by `StreamsStore._applyCloseCleanup` — close events, stale teardown, and peer-leave, for media and both screen-share directions; `staleTeardownPlan` is deleted (absorbed as the stale rows); the superseded-close `_iceTimings` leak died as a table row. Transport `error` events are **forensic-only** by amended design (§8 item 1's Amended block in the assessment): errors are symptoms, phases are verdicts — `ConnectionManager` forwards FSM error events (library change, CHANGELOG row), `FsmTransport` emits them minus blocked-transition records (declared decision, pinned with a negative control in `fsm-transport-error-forwarding.test.ts`), and the store handlers log `FsmError`/`SupersededError` with the root-cause text and touch nothing — no slot write, no transport close, no view event; the log-only invariant and the forensic-survival-through-phase-close pins live in `streams-store-wiring.test.ts`. The field-visible change from the error work is additive only (negotiation/data-channel exception text now reaches exported logs; the path was producer-less from Phase 3 until this round, so no teardown behavior rode it). `attributeSlotEvent` (`transport/media-event-policy.ts`) is the ONE supersede/no-slot guard — `decideSlotWrite`'s write-bearing arms delegate to it and the log-only handlers use it for attribution. WebRTC eligibility has ONE predicate: `decideWebrtcEligibility` (`transport/carrier-coverage.ts`), symmetric by decision — declared change: a node with the conversation module inactive now ignores inbound video InitRequests. `_peerModuleStates` has ONE merge rule: `decideModuleStateMerge` (`ui/src/module-state-policy.ts`) — `updatedAt` LWW for set AND delete, all stamps on the sender's clock; a pong sweep deletes an absent module only if the pong's `moduleStatesAt` stamp (new additive `PongMetaDataV1` field, stamped at both pong-meta build sites) is strictly newer than the entry, which kills the in-flight-pong module flicker; a legacy pong with no stamp and no entries keeps the pre-change unconditional sweep; declared change: a stale-stamped push is ignored. View: `shareMaximizeKey` (`room/modules/registry.ts`) is the one share-tile key constructor — stopping your own maximized screen share no longer blanks the room (pinned red-first in `screen-share-maximize-key.test.ts`); the retro's four leak shapes are fixed at their remaining instances and pinned (`view-teardown-symmetry.test.ts`, plus `logs-graph-teardown.test.ts` which mocks plotly instead of the module so the super-call pin cannot be hidden), including the resize-drag re-entry guard from review F3; paint reads authorities — room-view's two payload re-parses route through `parseConversationPayload`/`StreamsStore.webrtcDisabled` (declared changes: carrier icon agrees with the gate; a deactivated module no longer suppresses the meter) and the stats-panel flow glyph is `decideFlowGlyph` (`transport/carrier-stats-policy.ts`; declared change: WebRTC rx keys on `connected`); the four dead settings setters are deleted with the knob-timing semantics declared in the panel and pinned (`settings-path.test.ts` — TURN edits apply to new connections, signal delay on next join). Docs: `TRANSPORT_REFACTOR_PLAN.md` and `docs/WEBRTC_PEER_SIZE_AUDIT.md` are HISTORICAL (second drift).
- **§9 items 1–6 facts (landed 2026-08-05, six branches merged `--no-ff`, final merge `ff51cb4`; adversarially reviewed, no findings).** The signals-encoder retry contract: a failed `startCapture` (resolved-false OR rejection — the rejection arm was the live wedge) resets the running flag, and the per-presence-tick `_signalsTargets` subscription re-invokes the reconcilers — that per-tick firing is load-bearing, pinned by the encoder-retry wiring tests in `streams-store-wiring.test.ts`; if store notifications ever get memoized those tests go red and the retry needs an explicit home. The pong-echo RTT fold is `foldSignalsRtt` (`transport/carrier-stats-policy.ts`; `SIGNALS_RTT_PLAUSIBLE_MAX_MS`, `SIGNALS_RTT_EWMA_ALPHA` — both declared NOT-liveness, display predicates only). `handleInitAccept`'s SDP-exchange timer is tracked (`_sdpTimeoutTimers`) and attempt-scoped — a successor attempt disarms and survives the predecessor's deadline, `disconnect()` disarms all; the media `signaling` apply half is ONE method, `_applyMediaSignalingRoute`, used by the transport event glue AND the initiator path (the InitAccept hand-written slot write and its provably-dead supersede block are deleted); peer-leave clears `_lastDisconnectTime`/`_lastReconcileTime` as `closeCleanupPlan` rows, so a rejoining peer starts without the departed session's cooldowns (close events keep the stamp — that is the retry-gap semantics). RTCMessage action frames have one send seam: `encodeRtcAction` (`rtc-message-policy.ts`, beside the decoder) through `StreamsStore._sendRtcAction`/`_broadcastRtcAction` — future per-peer send policy goes there. The `S I M P L E   P E E R` banner and all `####`/`###`/`&&&&` marker logs are deleted (console only; the PresenceLogger pipeline is unchanged). `packages/webrtc-peer` is versioned 0.4.0 with the CHANGELOG banked, and 0.4.0 was published to the npm registry 2026-08-05 (registry state is checked with `npm view @lightningrodlabs/webrtc-peer version`, never trusted from prose).
- **View-round facts (§9's view-layer round; landed 2026-08-06, seven branches merged `--no-ff`, final merge `97d8b97`; adversarially reviewed — one process finding, the undeclared-rider sweep, resolved by a stack rewrite; `docs/section-10-meta-review` was reviewed and merged 2026-08-06 — see the meta-review bullet below).** The cross-pane room-ownership protocol has ONE home: `ui/src/room-ownership.ts` (`RoomOwnership` over injected `{getLocks, openChannel, clock}` seams; `decideHolderMessage`/`decideTakerMessage` pure; `TAKEOVER_ACK_TIMEOUT_MS`/`TAKEOVER_LOCK_WAIT_MS` named, declared handshake-timing NOT liveness) — presence-app keeps only the outcome→view-state mapping, and the ack-BEFORE-release ordering is a pinned protocol invariant (`room-ownership.test.ts`). Stream-onto-`<video>` has ONE implementation: `bindVideoStream` (`room/video-bind.ts`, modes `ensure` = never interrupt an already-bound element / `restore` = null-first forced re-attach; the 50/100/200ms delays are named paint timers pinned per call site — declared change: every bind sets autoplay and calls `play()` with swallowed rejection); room-view has zero raw `srcObject` writes (source-pinned). The room grid's tile count is `gridTileCount` (`room/layout.ts`) via room-view's one `_tileCount()`; the audible counter is `countAudiblePeers` (`peer-link-policy.ts`) with a reason-tagged basis — self-links beat observer-links beat the DECLARED legacy `Connected` fallback, and an empty broadcast beats the fallback. `peer-stats-panel` resets its render cache in `willUpdate` on pubkey reassignment (repeat() reuse). `agent-connection-status-icon.ts` is under the no-ambient-clock pin with an exactly-one-wall-clock-call relaxation (cross-peer wire comparison — declared correct timebase). `<agent-avatar>` is OUR `lobby/agent-avatar.ts`, imported directly by room-view and held by a registry-identity pin (`agent-avatar-registration.test.ts`) — never import the profiles library's same-tag element. `_reconnectAudio` and its mp3 are deleted. Process lesson recorded in §9's status block: diff the staged set before every commit — `git add -A` in a shared working tree swept a concurrent session's edits into a feat commit.
- **Meta-review ratified and the assessment demoted (2026-08-06; §10 merged `--no-ff` after its own adversarial review — measurements independently reproduced at the branch base; two immaterial discrepancies recorded in the merge message).** `MAINTAINABILITY_ASSESSMENT.md` carries `Status: HISTORICAL`: a dated record, no longer corrected in place — the review's two staleness findings were dissolved by the demotion rather than patched, on the document's own §7.3/§10.3 diagnosis that patching dated prose is the rot pattern. Ratification by the room owner: rec 1 (the paydown loop is closed) adopted; rec 2 moot (the view round landed the day after measurement); rec 4 adopted in strengthened form (whole-document demotion, not just freezing the round record); rec 3 (field-defect stop rule) not adopted — no standing loop remains to schedule rounds; rec 5 (consolidating this file's fact bullets) deferred until feature traffic has run. The dormant-until-trigger list lives HERE now: `StreamsStore.onEvent`'s single overwritable callback slot — first second-consumer feature; settings unification — only if a live-apply decision reverses the settings-panel delete-and-declare; a third signals-carried media type — a phase, by declaration; eslint repair — inside the EOL-deps decision; Rust validation and `tests/` workspace version alignment — the 0.7 upgrade; conductor-level (real-Holochain) validation — manual, by declaration.
- **Holochain 0.7 step-1 facts** (landed 2026-08-03 on `main-0.7`; adversarially reviewed — findings 1 and 2 applied in the review fixup; rebased onto the post-§10 `main-0.6` 2026-08-06). This branch is the 0.7 upgrade line: hdi 0.8.0 / hdk 0.7.0 / holonix `main-0.7`, zomes on the ActionHeader+ActionData integrity API, devshell node is `nodejs_22` (nodejs_20 fails nix evaluation on the 0.7 pin). The DNA test framework is `@holochain-open-dev/tryorama` ^0.20.0 — the decision rationale and the known validation-`Invalid` coverage gap live in `tests/README.md`; `tests/src/room/room.test.ts` is the DNA gate content, run by the `dna-tests` CI job (root `npm test`: build:zomes, `hc app pack`, `npm t -w tests`). Declared consequence: the DNA hash changed — 0.7 agents form a separate network from 0.6 agents. Step 2 (the UI client migration) landed 2026-08-06: the ui workspace is on client ^0.21.0 / open-dev ^0.700.0 / `@theweave/api` 0.7.0-dev.1 (`@theweave/elements` deleted — zero imports existed; only dev releases of the 0.7 weave API exist, and the Moss host must itself run Holochain 0.7). The one code change was `RoomSignal` (`ui/src/types.ts`): its five hand-written action-bearing variants — broken by client 0.21's `Action` → `{header, data}` restructure — were replaced by the library authority `ActionCommittedSignal<EntryTypes, LinkTypes>` from utils 0.700 (review finding: the hand-copy was the parallel-copy-that-drifts pattern; it had already drifted, omitting `LinkDeleted`'s `create_link_action` which the zome always sent), with `EntryTypes`/`LinkTypes` typed to mirror the integrity zome's serde-tagged enums instead of `{}`. Type-level only: UI runtime signal consumers match `Message`/`Pong` exclusively, and nothing reads action internals off `EntryRecord`.
- `nix develop -c npm run verify` is the gate: both workspaces' unit suites plus a `tsc --noEmit` of both — since the 2026-08 retro round at `strict` PLUS `noUnusedLocals`/`noUnusedParameters` — in a few seconds. CI runs the same command on push and PR to `main-0.6` and `main-0.7` (`.github/workflows/test.yaml`, since the 0.7 step-1 chain). The suite count is deliberately not recorded here: it is the run's own output, stale copies of it in this file broke twice, and `claude-md-drift.test.ts` now rejects numeric test-count claims in this file; per-phase count history lives in the assessment (§7.2). (Phase 0 landed the gate; the `ui/` typecheck is green only against a built `packages/webrtc-peer` — `verify` builds it first.)
- `StreamsStore` is **constructible AND startable** under `vitest` in the node environment (Phase 2 item 2 + Phase 6): the constructor only assigns fields and defines derived stores; everything ambient lives in `start()`, which since Phase 6 runs against the injected deps record. `ui/src/__tests__/streams-store-construction.test.ts` guards the constructor; `streams-store-wiring.test.ts` exercises the started store's glue. Still prefer extracting pure decision functions over characterization-testing the class — the wiring suite asserts the apply halves, not the decisions.
- **Post-Phase 1 carrier facts.** Signals carries a present peer unless WebRTC is `connected` (ICE + DTLS up) — the complement of *media flowing*, not of *an attempt existing*; the rule is in `ui/src/transport/carrier-coverage.ts`. "Present" there means the **present predicate** (`_presentPeers`), not ping-freshness: keying the send set on `_activeAgents` alone was §3.1(b) one-way audio, fixed in the PR #4 review pass. Every `ConnectionPhase` is routed by `routeTransportPhase` (`transport/media-event-policy.ts`), exhaustive over the union, with `failed`/`idle`/`closed` clearing the slot and `signaling` adopting a new `connectionId` when the transport replaced an FSM underneath us — **`ConnectionManager.fsm.destroy()` emits no transition, so a connection can disappear with no event at all**; never assume you will be told. The FSM owns transport recovery and the pong-driven teardown stands down (`transport/stale-connection-policy.ts`) — since Phase 3 that is every link. A link gets WebRTC at all only if the peer's payload declares (or, for pre-caps builds, is inferred to hold) the `sdp-fsm` capability — `conversationPayloadCaps` is the one capability read (`StreamsStore.webrtcAvailableFor`). **Field validation is partial** (Phase 1.5): the transport stack and carrier policy are exercised nightly against real RTCPeerConnections — establishment, silent-drop flap, handover in both directions — by `ui/harness/carrier-handover.spec.ts`; since Phase 3.5 the screen-share FSM path is too (`ui/harness/screen-share.spec.ts` — lazy establishment over `SdpFsmScreen`, dir-routing under mutual share, malformed-dir drop, teardown on silent drop). Since Phase 6 the store's own slot-apply and routing wiring — media and screen-share both, per review F1 — is validated in node (`streams-store-wiring.test.ts`), and since Phase 6.5 the harnesses construct the real store, so the nightly falsifies that same wiring against real ICE/DTLS (the mirrored dispatch glue is deleted); no conductors run anywhere, so conductor-level validation remains manual. Known bounded exception to the carrier invariant (recorded 2026-07-28): during `reconnecting`/`disconnected` the slot keeps `connected: true` (`routeTransportPhase` → `transport-owns-recovery`), so a peer can be off signals with no media flowing for the length of the transport's recovery window — and clearing a wedged FSM slot depends entirely on the FSM emitting `failed`.
- **Cross-version interop with ≤ v0.14.7 is a declared non-goal** (business decision, 2026-07-28); Phase 3 extended the consequence to v0.14.8 screen share (declared in its section of the assessment). The capability gate (`webrtcAvailableFor`; formerly `resolveWebrtcImpl` rule 0) is what enforces it. *Forward* interop is guarded by Phase 1.5's wire-contract gate — wire surface declared once and snapshot-tested, a per-release compat-corpus fixture, capability declared in the payload (`caps`) rather than inferred by field-probing — not by building old versions.

## Target state (partially reached — check per predicate)

The plan in `MAINTAINABILITY_ASSESSMENT.md` §5 converges liveness onto four predicates with one authority each. Status: **present** and **media-flowing** have single authorities since Phase 2 (`computePresentPeers` / `isMediaLive` in `presence-policy.ts`); **carrier-active** since Phase 1 (`carrier-coverage.ts`); **reachable** since Phase 4 — the buckets (`lastSeenBucket`) are documented as the signals-reachable predicate, and the one place its precedence against WebRTC `connected` is decided is `decideAudioLink` (`peer-link-policy.ts`, flow beats reachability). All four have landed.

## Where knowledge goes

**Types > tests > this file > prose.** Push every invariant as far up that list as it will go. Anything that lands in prose is temporary and will rot silently — 17 assertions in this repo's documents were verified false while reading as current fact. The insights were right; the storage medium was wrong.

## The unit of change

Decisions go in pure functions: snapshot in, tagged union out, carrying a `reason`. Table-driven tests, no mocks. `ui/src/transport/media-event-policy.ts` is the template — copy its shape. (The former template, `auto-flip-policy.ts`, was deleted with SimplePeer in Phase 3.)

- **Exhaustive `switch` over a union type**, so an unhandled case is a compile error rather than a silent drop. `_dispatchMediaEvent` dropping five of eight `ConnectionPhase` members is the bug this prevents.
- **One authority per concept, one exported name.** A second implementation must delete the first. Adding a parallel path is the cheap wrong move and is how this codebase acquired 21 liveness sources.
- **Every important mock needs a negative control** — a test that fails if the mock cannot reproduce the bug the mock exists to catch. The motivating specimen: at the 2026-07 assessment `MockRTCPeerConnection` could not throw, so the `InvalidStateError` guard in `rtc-peer.ts` had no coverage and an inversion would have survived. The throwing-mock suite in `rtc-peer.test.ts` ("duplicate-answer InvalidStateError guard") closed that and is the template. (This bullet itself went stale as a negation — corrected 2026-08-03; see the "True today" contract.)
- **Chase decision coverage, not line coverage.** At the 2026-07 assessment the library sat near-fully covered while the orchestrator had no tests at all — and every production bug had been in the orchestrator. The wiring and construction suites closed the zero; the principle stands: coverage of decisions, not lines, is what predicts field behavior.
- **An abstraction not used as a type annotation gets deleted, not kept — or made real.** `PeerTransport` looked like a constraint and imposed nothing for three phases; Phase 4 made it the annotation on the store's transport fields, which is what turned "don't reach into the pc" from a review rule into a compile error.

## Working agreements

1. **Replace or declare.** Every change either names the mechanism it replaces, or states explicitly that it adds a parallel one and why. "Runs in parallel with X; X remains the source of truth" is how this codebase acquired four parallel models — it requires a justification, not a passing mention.
2. **No new threshold without a named predicate.** Six liveness clocks arrived one constant at a time. A new timeout must say which predicate it serves and reuse that predicate's clock.
3. **Prose cites code or gets deleted.** An invariant comment names the `file:line` it constrains. Active documents are corrected in place or deleted — no status ritual; a superseded or historical document MUST carry a `Status: SUPERSEDED (see X)` / `Status: HISTORICAL` header, following `docs/WEBRTC_PEER_CLAIM2_DATACHANNEL_GATING.md`. (Decided 2026-08-03; this rule previously demanded a header on every document while Phase 0 item 6 recorded that mechanism as rejected — the retro found the contradiction, and the practice won.) A design doc marks each item landed or not-landed once it ships. Living prose cites files and tests, not commit hashes — the Phase 1 branch rewrite invalidated four hash citations.
4. **Decisions become pure functions before they become complicated.** `ui/src/transport/media-event-policy.ts` is the template: plain-object in, tagged union out, table-driven tests, no mocks.
5. **The gate runs, or it isn't a gate.** A disabled workflow, an unrun typecheck, and an unenforced `strict` flag are all recorded intentions, not constraints.
6. **One intent per branch.**
7. **A conclusion may not dismiss field evidence on the strength of passing tests.** This happened (`12cb027`, then `c143cda` six weeks later proved it wrong) — at the time the mocks could not throw, so the suite was structurally unable to reproduce the field failure it was being cited against. Passing tests bound what the suite can see, never what the field can do.
8. **A fix names the symptom's previous fixes.** Use "supersedes `<hash>`" in the commit message. The same pane-survival symptom was fixed three times in three layers over 71 days, and the third broke an invariant the first documented.
9. **Every phase PR gets an adversarial review by a session that did not write it**, reading only the diff and the code. The second cause of the §3.1(c) wedge was found exactly this way, before the field found it. The authoring session's self-review converges to agreement with itself; independence is the property that caught it.
